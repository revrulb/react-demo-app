/* eslint-disable camelcase */
import { createUseStyles } from "react-jss";

export const useSharedStyles = createUseStyles({
    filters__control: {
        margin: "10px"
    },
    "request-tape__item__header": {
        "text-align": "center"
    }
});
